# ZooKeeper

## 摘要和引言

协调内核：提供一个简单的 API。

无等待：不管别人在做什么，保证高性能

保证：FIFO or 线性化写入

分布式系统需要不同的协调服务，例如领导者选举等等。

Zookeeper 直接不用锁，提供一个类似文件系统的操作，要用锁的话可以使用简单的 API 在自己的客户端实现。

Zookeeper 读写分离，读在本地化操作，写需要全局排队，且具有流水线，可以一次性发多个操作过去

Zookeeper 有监视机制，帮忙 Watch 有没有变化，如果变了，不负责更新缓存，只通知你。
